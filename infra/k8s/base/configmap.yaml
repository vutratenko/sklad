apiVersion: v1
kind: ConfigMap
metadata:
  name: sklad-api-config
data:
  APP_ENV: production
  HTTP_ADDR: ":8080"
  AUTH_DEV_BYPASS: "false"
  OIDC_REDIRECT_URI: "https://sklad.example.com/oauth/callback"
