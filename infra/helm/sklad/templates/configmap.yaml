apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "sklad.fullname" . }}-api-config
  labels:
    {{- include "sklad.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.api.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
